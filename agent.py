class BaseAgent:
    """
    Base definition for an Agent.
    In this architecture, agents are independent modules exposing 
    an analyze(input_json_str: str) -> str function that takes and returns JSON.
    """
    pass
